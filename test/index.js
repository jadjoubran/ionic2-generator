var should = require('chai').should(),
    ionicHandler = require('./src/core/ionicHandler'),
    angularHandler = require('./src/core/angularHandler');

beforeEach(() => {

});

describe('Ionic page generation') {

}